# auto-generated by grc.converter

id: keyfob_keyfob_encode
label: keyfob_encode
category: '[Keyfob]'

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import keyfob
    make: keyfob.keyfob_encode()

file_format: 1
